<Page x:Class="testMvvm.View.Windows.DriverPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:testMvvm.View.Windows"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DriverPage">

    <Grid>
        <DataGrid x:Name="MyDG" 
    RowStyle="{DynamicResource DataGridRowStyle1}"
        ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}"
        CellStyle="{DynamicResource DataGridCellStyle1}"
        Style="{DynamicResource DataGridStyle1}" Margin="36,37,108,13" d:ItemsSource="{d:SampleData ItemCount=5}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding name}"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding count}"/>
            </DataGrid.Columns>

        </DataGrid>
        <Button Style="{StaticResource MyButtonStyle}" Height="40" Margin="552,18,0,0" Width="110" HorizontalAlignment="Left" Content="add" FontSize="20" Command="{Binding CreatAddWindowCommand}"/>

    </Grid>
</Page>
